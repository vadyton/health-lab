services:
  app:
    image: ${DOCKERHUB_USERNAME}/health-lab:latest
    platform: linux/amd64
    ports:
      - "${PORT:-3333}:3333"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      PORT: 3333
    restart: unless-stopped
